import React from 'react'; import Header from '../components/common/Header';
export default function SupportPage() { return (<><Header title="Support" /><div className="page-body"><div className="card" style={{textAlign:'center',padding:'60px 20px',color:'var(--text-muted)'}}>💬 Support ticket system — submit queries and get responses from admin/moderator.</div></div></>); }
